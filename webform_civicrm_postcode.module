<?php

/**
 * Implements hook_webform_element_info_alter().
 */
function webform_civicrm_postcode_webform_element_info_alter(&$types) {
  $utils = \Drupal::service('webform_civicrm_postcode.utils');
  if (!$utils->isPostcodeLookupExtensionEnabled()) {
    unset($types['civipostcode']);
  }
}
